[Local System Prompt]:{
  You are acting as a senior software engineer helping build a small, production-ready academic project.

  Rules you must follow:

  Do exactly what is requested — do not add extra features.
  Do not change the given architecture or file structure unless explicitly told.
  Ask for clarification before coding if any requirement is ambiguous.
  Prefer clarity and correctness over cleverness.
  Keep the solution simple, readable, and demo-ready.
  Output complete, ready-to-run files when asked.
  Do not introduce new technologies unless explicitly approved.
  Treat this as a final deliverable, not a draft.
  No assumptions
  No placeholders
  No pseudo-code
  No unnecessary explanations
  If something is missing, stop and ask first.
  If anything is ambiguous, ask before coding.
}
[context]:{

  Project Overview
  ----------------
  SHRDAA (Secure High-Integrity Registry for Data Audit & Authentication)
  is a concept demonstration project that showcases how blockchain technology
  can be used as an immutable audit layer on top of a traditional centralized
  financial system.

  The project simulates a government fund-flow environment where transactions are
  handled by a centralized backend, while their cryptographic hashes are permanently
  recorded on a blockchain. This ensures tamper detection, integrity verification,
  and auditability without replacing existing systems.

  The objective is to demonstrate the core value of blockchain in improving trust
  and transparency, rather than building a full-scale government or banking solution.

  The FILE SYSTEM of the project is going to be as follows :

 Project/
    coresystem.py 
    database.py 
    authorisation.py
    config.py
    main.py 
    templates/
      index.html 
    database/
      accounts.csv
      ledger.csv
      blockchain.csv
      project_dis.csv

 You are ment to stick to this file system while coding.

 The following tells you what each file in the file system is going to do.

 coresystem.py: 
    This is where all the main system processing will take place.
    Invovling the following processes:
      - Adding and Removing Differnt Types Users (Types are going to be specified in the config.py file).
      - Hashing transaction data taken from the user and giving it to the database.py to add it to the database.
      - Bundling transaction data and passing it on to the database.py to add it to the database.
      - Collecting data from the database.py to give to the main.py when required.
        - E.g. when a users wants to view transactions in one project, this modules tells database.py to fetch that data 
               and gives it to the module requesting it.
               (The features the UI will have will be give bellow, design accordingly)
      - Verify transactions by collecting transcation data form the database.py and rehashing it and comparing it with the 
        stored hashes.
      - All other necessary feature that are required by the UI(index.html)
 database.py: 
    This is the module that handles the database/ and the csv files inside.
    Will have all the necessary features that are required by the coresystem.py and authorisation.py .
 authorisation.py:
    This is the module that authorise the logins and transaction.
    Login authorisation: 
      In the ( [Page] Login ) if account_no and password(after hashed) are matched then approves to show the respective
      ([Page] DashBoard ).
      A local temp veriable is created to store login status (yes(account_no)/no)
    Transaction authorisation:
      While a transaction is being made the password(after hashed) is collected and is paired with the account_no in the 
      local temp veriable, then comparision of account_no and password(after hashed) stored and collected is done to approve.
      (if approved a small pop up on the bottom right says transaction completed else transaction failed)
 config.py:
    This is the module that holds system configs.
    Will have all the default file paths etc.
    Will have all the User types.
    Will have all the default balances etc(100000000 for Govt_officer / 1000000 for Beneficiary).
 main.py:
    This is the main entry point to the application, uses flask to host the index.html(html+css+js, all allowed as long as its stays in a single file) .
 templates/:
    This dir that holds the index.html
 index.html:
    [Style]: 
      - Modern sleek UI, which mimics the Indian government's official websites to show that this project means bussiness.
      - Smooth animations to have a touch of freshness and show that this is not a sloppy system.
      - There should be a pannel on the top holding the Name of the project (when clicked should go to [Page] Home)
        on the left and pages involved in the right. This should stay as it is when any page is visited.
      - All popups should have a close button on their tile and should stay for 5 seconds.
    [Features and pages]:
      - [Page] Home : Has a Overview of what this website is about.
      - [Page] Public Ledger : Displaies all the transactions grouped into projects that are present in the ledger.csv fully.
        - [Feature] Projects : Shows all existing projects displayed on small tiles/pannels in a grid holding Brief discription of the project bellow its name.
          - [Page] Project : Once the project name is clicked it should open a dedicated page showing all the transactions invovled.
      - [Page] Login : A login page for those who have access. Once logged in should enter the DashBoards.

        - [Page] DashBoard : Every User will have a DashBoard to see personal details and other relevent features and information.

          - [Page] Govt_officer DashBoard :
            -[Feature] Has a tile/pannel (narrow) on the left holding all the personal information 
            (name , age , rank , location , no_of projects invovled in , account_no , balance) 
            -[Feature] Has a tile/pannel (wide) holding a Gird of tiles/pannels that shows all the invovled 
              (invovlment means has done a transaction in the project) projects.
              - [Page] Project : Once the project name is clicked it should open a dedicated page showing all the transactions invovled on a tile/pannel(wide).
              - [Feature] Once inside a project page, the tile/pannel the transactions are on should have two button that says (Make Payment) and (New Project) .
                - [Page] Make Payment : Once clicked on (Make Payment) it should open a page that gives a form 
                (to_account_no (if the account_no is of another Govt_officer or a Auditor then display a pop up that says invalid account_no) , project_no , 
                , amount , passowrd (if entred wrong show a wrong password popup on the bottom right cornor))
                - [Page] New Project : Once clicked on (New Project) it should open a page that gives a form 
                (account_no s (the form should be a huge tile with a input block, once account_no is 
                entred their name shold be shown on a small tile on the huge tile) , project_disscription (to be added to the project_dis.csv alsong side a 
                project_no generated by the coresystem.csv)
            -[Feature] Has a tile/pannel (narrow) on the right to creat a new user by filling out a form with appropriate details like 
            (name , age , {rank (if a Govt_officer (District Collector , Secretary , Joint Secretary), if a auditor(Auditor)} / {company (if a Beneficiary)}, 
            location , password , balance ). ## i know this is not good to be done this way but this is a demo project 

          - [Page] Beneficiary DashBoard :
            -[Feature] Has a tile/pannel (narrow) on the left holding all the personal information (name , age , company , location , account_no , balance )
            -[Feature] Has a tile/pannel (wide, filling out the rest of the space) that shows all the invovled 
            (invovlment means has done a transaction in that project) projects.

          - [Page] Auditor DashBoard : 
            -[Feature] Has a tile/pannel (narrow) on the left holding all the personal information (name , age , company , location , account_no)
            -[Feature] Has a Gird of tiles/pannels that shows all the existing projects similar to the [Page] Public Ledger.
              - [Page] Project : Once the project name is clicked it should open a dedicated page showing all the transactions invovled.
              - [Feature] For each transaction there should be a dedicated button that reverifies each transaction by reshasing it and 
                comparing it to the exisiting hash (should give a appropriate popup on the bottom right saying (verified/chain-missmatch)).
      - [Page] About : A page that further give a ultra detailed insight on how this system is better than existing systems.
 database/:
    This will most be updated by a single user at a time.
    This dir will contain the accounts.csv ledger.csv blockchain.csv
    accounts.csv: 
      name , account_no(generated by coresystem.py when account is being created) , password(hash, free to use any safe algo) , balance , age , location ,
      {rank (if a Govt_officer (District Collector , Secretary , Joint Secretary), if a auditor(Auditor)} / {company (if a Beneficiary)}, 
      authorised_projects_no (while creating a project different account numbers can be added to a project, 
      if said account_no is added in any project then it is to be stored here) 
    ledger.csv:
      transaction_no (generated by coresystem.py when transaction is done), project_no , from_account_no , to_account_no , ammount , timestamp , verification_status
      ((done(one time verification, can only be done if verification_status == pending, any Auditor can do it)/pending))
      # not grouped into projects just entred one after the other
      # Balance while doing said transaction can go negitive after transaction if it does (the index.html must show invalid transaction popup )
      # Govt_officer -> Govt_officer/Auditor transaction is not allowed 
    blockchain.csv:
      transcation_no , project_no , previous_hash , current_hash
      # not grouped into project just entred one after the other similar to how transaction are entred in the ledger.csv
    project_dis.csv:
      project_no , project_disscription(as a string)
 }

[Prompt]:{
  Based on the [context] given to tell me do you have everything ready to starting building this project. 
  The process is going to be done in this order:
    architecture finilisation.
    coding module wise.
    implimentation.
    testing.
    disscussion on how the demo should be done.
}
